# Frontend — API only (no user auth)
VITE_API_URL=http://localhost:8000

# Optional: fallback for building public storage URLs if API omits them
VITE_SUPABASE_URL=

# API server (api/.env or process env)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
